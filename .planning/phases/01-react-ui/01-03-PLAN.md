---
phase: 01-react-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [electron/src/App.tsx, electron/src/vite-env.d.ts]
autonomous: true
requirements: [DESIGN-02]
gap_closure: true
must_haves:
  truths:
    - "No Spider icon references anywhere in App.tsx"
    - "TypeScript compiles without errors"
    - "All Phosphor icon imports match available icons in v2.1.10"
  artifacts:
    - path: "electron/src/App.tsx"
      provides: "Crawling UI with corrected icon references"
      contains: "Bug"
    - path: "electron/src/vite-env.d.ts"
      provides: "CSS module type declarations for Vite"
      contains: "declare module '*.css'"
  key_links:
    - from: "electron/src/App.tsx"
      to: "@phosphor-icons/react"
      via: "icon imports"
      pattern: "from.*@phosphor-icons/react"
---

<objective>
Fix Spider icon reference bug and add CSS module type declarations to resolve TypeScript errors.

Purpose: Eliminate runtime crash when markdown output renders and clean up TS compilation errors.
Output: App.tsx with all icons properly imported, vite-env.d.ts for CSS type declarations.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-react-ui/01-VERIFICATION.md
@.planning/STATE.md
@electron/src/App.tsx
@electron/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Spider icon reference in App.tsx</name>
  <files>electron/src/App.tsx</files>
  <action>
    Read electron/src/App.tsx and find ALL occurrences of "Spider" (both imports and JSX usage).

    Replace every <Spider /> or Spider reference with <Bug /> (which is already imported on line 3).

    Also verify the import statement on line 3 — ensure Spider is NOT in the import list. If it is, remove it. The import should only contain: Bug, Globe, Link, ArrowRight, Spinner, CheckCircle, Warning.

    Verify no other icon names are referenced without being imported.
  </action>
  <verify>cd electron && rg "Spider" src/App.tsx; echo "exit: $?"</verify>
  <done>No "Spider" string found in App.tsx (neither imports nor JSX). All icon references match imported names.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS module type declarations for Vite</name>
  <files>electron/src/vite-env.d.ts</files>
  <action>
    Create electron/src/vite-env.d.ts with CSS module type declarations:

    ```typescript
    /// <reference types="vite/client" />
    declare module '*.css' {
      const content: Record<string, string>;
      export default content;
    }
    ```

    The `/// <reference types="vite/client" />` directive provides all Vite built-in type declarations including import.meta.env and CSS imports.
  </action>
  <verify>cd electron && npx tsc --noEmit 2>&1 | head -10</verify>
  <done>TypeScript compiles with zero errors. vite-env.d.ts exists with Vite client reference and CSS module declarations.</done>
</task>

</tasks>

<verification>
- rg "Spider" src/App.tsx returns no matches
- npx tsc --noEmit returns zero errors
- npx vite build succeeds
- All Phosphor icon imports match available icons in @phosphor-icons/react v2.1.10
</verification>

<success_criteria>
- App.tsx has no Spider icon references (import or JSX)
- All icon imports match the icons actually used in JSX
- TypeScript compilation is clean (zero errors)
- Vite build succeeds
- vite-env.d.ts provides CSS type declarations
</success_criteria>

<output>
After completion, create `.planning/phases/01-react-ui/01-03-SUMMARY.md`
</output>
