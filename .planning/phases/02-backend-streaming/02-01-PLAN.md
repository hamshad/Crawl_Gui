---
phase: 02-backend-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend.py
requirements:
  - STREAM-01
  - STREAM-03
  - STREAM-04
autonomous: true
must_haves:
  truths:
    - "SSE endpoint /crawl/stream accepts POST requests with JSON body containing url"
    - "SSE response has correct headers: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no"
    - "Required imports (Response, json, threading, Queue) are present in backend.py"
  artifacts:
    - path: "backend.py"
      provides: "SSE endpoint route definition"
      contains: "@app.route('/crawl/stream', methods=['POST'])"
    - path: "backend.py"
      provides: "Flask Response import"
      contains: "from flask import Response"
    - path: "backend.py"
      provides: "JSON module import"
      contains: "import json"
    - path: "backend.py"
      provides: "Threading module import"
      contains: "import threading"
    - path: "backend.py"
      provides: "Queue import"
      contains: "from queue import Queue"
  key_links:
    - from: "backend.py"
      to: "electron/src/App.tsx"
      via: "HTTP POST SSE /crawl/stream endpoint"
      pattern: "fetchEventSource.*crawl/stream"
---

# Plan 02-01: SSE Endpoint Setup and Imports

## Goal
Add required imports and create the `/crawl/stream` SSE endpoint skeleton in Flask backend.

## Tasks

<task id="1" type="auto">
<name>Add SSE and threading imports to backend.py</name>
<action>Add `Response` to the existing `from flask import` statement. Add `import json`, `import threading`, and `from queue import Queue` to the top of backend.py.</action>
<files>backend.py</files>
<verify>Run `grep -n "from flask import Response" backend.py` and `grep -n "import json" backend.py` to confirm imports exist.</verify>
<done>All required imports are present at the top of backend.py: `Response` in flask imports, `json`, `threading`, `Queue`.</done>
</task>

<task id="2" type="auto">
<name>Create /crawl/stream POST route with SSE response</name>
<action>Add a new route `@app.route('/crawl/stream', methods=['POST'])` that accepts JSON body with `url`, creates an `event_queue = Queue()`, starts a background thread targeting `run_crawl` (implemented in 02-02), and returns a `Response` with `mimetype='text/event-stream'` and headers `Cache-Control: no-cache`, `X-Accel-Buffering: no`. The response body calls the `generate(event_queue)` function (implemented in 02-02).</action>
<files>backend.py</files>
<verify>Curl `curl -N -X POST http://127.0.0.1:5001/crawl/stream -H "Content-Type: application/json" -d '{"url":"https://example.com"}'` returns a response with Content-Type text/event-stream.</verify>
<done>Route `/crawl/stream` exists, returns Response with correct mimetype and headers, creates event_queue and starts background thread.</done>
</task>
