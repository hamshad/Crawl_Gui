---
phase: 02-backend-streaming
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - electron/src/App.tsx
requirements:
  - STREAM-02
  - STREAM-03
  - STREAM-04
autonomous: true
must_haves:
  truths:
    - "handleCrawl uses fetchEventSource to POST to /crawl/stream and handle SSE events"
    - "Real-time logs are appended to state as SSE log events arrive"
    - "Progress indicator and isStreaming state track SSE connection status"
    - "Error handling for SSE connection failures is implemented"
  artifacts:
    - path: "electron/src/App.tsx"
      provides: "updated handleCrawl with fetchEventSource"
      contains: "fetchEventSource("
    - path: "electron/src/App.tsx"
      provides: "isStreaming state variable"
      contains: "const [isStreaming, setIsStreaming]"
    - path: "electron/src/App.tsx"
      provides: "SSE onmessage handler"
      contains: "onmessage"
    - path: "electron/src/App.tsx"
      provides: "SSE onerror handler"
      contains: "onerror"
  key_links:
    - from: "electron/src/App.tsx handleCrawl"
      to: "backend.py /crawl/stream"
      via: "fetchEventSource POST SSE"
      pattern: "fetchEventSource.*crawl/stream"
---

# Plan 02-04: Frontend SSE Consumer Implementation

## Goal
Replace the polling-based handleCrawl with fetchEventSource to consume SSE events, add streaming state, and display real-time progress.

## Tasks

<task id="1" type="auto">
<name>Replace handleCrawl with fetchEventSource and add isStreaming state</name>
<action>Add `isStreaming` state variable (rename from `isLoading` or add alongside). Rewrite `handleCrawl` to use `fetchEventSource` POST to `/crawl/stream` with JSON body `{url}`. Add `onmessage` handler that parses event data and handles `log`, `progress`, `done` events. Set `isStreaming` true on start, false on done/error.</action>
<files>electron/src/App.tsx</files>
<verify>React component calls fetchEventSource and handles all SSE event types.</verify>
<done>`isStreaming` state exists, `handleCrawl` uses fetchEventSource, onmessage handles log/progress/done events.</done>
</task>

<task id="2" type="auto">
<name>Update UI for real-time streaming feedback</name>
<action>Update the UI to show real-time logs as they arrive (not just at end). Show `Progress` component with animated state during streaming. Keep "Crawling..." badge visible while `isStreaming` is true. Update loading state to use `isStreaming` instead of `isLoading`.</action>
<files>electron/src/App.tsx</files>
<verify>Logs appear incrementally during crawl; progress indicator animates while streaming.</verify>
<done>Logs are appended in real-time, Progress component shows animated state during isStreaming, "Crawling..." badge visible while streaming.</done>
</task>

<task id="3" type="auto">
<name>Handle SSE errors and connection cleanup</name>
<action>Add `onerror` handler to fetchEventSource that sets error state and stops streaming. Add cleanup logic for component unmount or manual stop to abort the SSE connection. Ensure no hanging connections on error or unmount.</action>
<files>electron/src/App.tsx</files>
<verify>Error state shown on SSE failure; no hanging connections.</verify>
<done>`onerror` handler exists, error state set on SSE failure, cleanup logic prevents hanging connections.</done>
</task>
